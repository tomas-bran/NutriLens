/** Enlaces externos del producto (paridad con la web). */

/** Sitio de documentación (Docusaurus) — guías paso a paso de cada función. */
export const DOCS_URL = 'https://nutrilens-docs.azurewebsites.net';
